// Modal Loader

import { View } from "react-native";
import ModalWrapper from "./AModalWrapper";
import ModalResponse from "./ModalResponse";


const ModalLoader = () => {



  return (
    <View>

      {/* <ModalWrapper modalKey="responseDiscardWorkout">
        <ModalResponse
          modalKey="responseDiscardWorkout"
          isInput={false}
          message="Discard Current Workout?" acceptLabel="Discard"
          onAccept={() => startSession()} />
      </ModalWrapper >

      <ModalWrapper modalKey="historyCalendar" animation="slide">
        <HistoryCalendar />
      </ModalWrapper> */}

    </View >
  );
}

export default ModalLoader;